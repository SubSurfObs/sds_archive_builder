# Network S1 — AuSIS, Australian Seismometers in Schools
# Served via AUSPASS. Public open dataset; publish lag is sub-hour.

network: S1
description: "AuSIS — Australian Seismometers in Schools"

channels:
  - "HHZ"
  - "HHN"
  - "HHE"
  - "BHZ"
  - "BHN"
  - "BHE"

location_codes:
  - ""
  - "00"

servers:
  primary: "https://auspass.edu.au"
  fallback: null

history:
  start: "2019-01-01"
  data_lag_days: 0

retry_policy:
  max_attempts: 5
  # Check more aggressively as data continues to come online
  retry_after_days: [7, 21, 60]
  error_backoff_max_days: 14
