name: sds_archive_builder
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - obspy>=1.4
  - sqlalchemy>=2.0
  - pyyaml>=6.0
  - click>=8.1
  - numpy>=1.24
  - requests>=2.28
  # Phase 2 — ASDF support (uncomment when needed)
  # - pyasdf>=0.8
  - pip
